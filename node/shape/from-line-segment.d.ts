import { Shape } from "./shape.js";
import { ShapeType } from "../types/shape-type.js";
declare function fromLineSegment(sides: ShapeType, ls: number[][], stagePlacement: number): Shape;
export { fromLineSegment };
